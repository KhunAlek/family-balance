# Fresh Chat Prompt — Step 4 Canonical Terminal Transaction Read Model

Copy the text below into a fresh ChatGPT coding chat and upload the accompanying Step 4 ZIP.

---

Work in the Family Cash Flow repository and implement **Step 4: Canonical Terminal Transaction Read Model** from the uploaded package.

The expected starting repository SHA is:

`bd369e00b16116c4be2ea70ea325efe6ac96747a`

First read repository `AGENTS.md` and every uploaded package document. Verify the package manifest, exact starting SHA, and dirty-worktree state. Inspect all relevant committed schema, typed factual readers, current accounting/reporting consumers, correction safety, portable backup/restore, Step 3 preflight, and tests at that exact SHA before describing current behavior.

I accept the Step 3 candidate and `STAGE_3_PREFLIGHT_DECISION_RECORD.md` as implementation authority for local Step 4 work. I authorize you to implement and test a canonical internal read-only terminal transaction model using local synthetic/repository fixtures, create a local candidate commit, produce the complete Step 4 result and execution ledger, and package all documents and the prompt required for a fresh chat to implement Step 5.

I do not authorize you to run against production or a representative production copy, write any classification or relationship to a database, apply or change migrations, backfill identities, repair or reinterpret historical facts, merge, deploy, mutate D1/R2, change Cloudflare configuration, add or upgrade dependencies, enable transaction-history UI or public read APIs, enable any transaction-management mutation, switch current financial consumers to the new model, or begin Step 5 or later work.

Use the terminal pointer as authoritative for Step 2 identities. Resolve complete same-transaction version/component/audit chains and fail closed on missing, crossed, duplicated, unsupported, or incomplete relationships. Adapt Step 3 unambiguous classifications read-only. Keep ambiguous legacy items separately visible and mutation-disabled, keep balance observations separate, and keep non-transactions excluded. Superseded, deleted, reversal, and management rows must never affect financial totals.

Define a stable canonical DTO and stable refusal codes covering logical identity, kind/lifecycle, terminal date/version/revision, description/source/payee, category, positive total satang, direction, allocations, typed components, creation evidence or explicit absence, audit summary, reconciliation state, and permitted actions. Output and serialization must be deterministic. Prove repeated and shuffled-input determinism, zero writes on success/failure, active/deleted/audit behavior, complete component reconstruction, ambiguity preservation, and fail-closed invalid chains.

Proceed continuously through implementation, verification, candidate commit, result, ledger, and Step 5 ZIP. Stop only for a genuine unresolved material rule, conflicting owner change that cannot be preserved, unavailable required evidence, or a required authorization gate. If a material rule is unresolved, ask exactly one focused question with the evidence requiring the decision.

At the exact candidate SHA, run focused Step 4 read-model tests, Step 3 preflight determinism/coverage/zero-write tests, Step 2 schema and portable backup/restore tests, Step 1 correction safety, and the complete Slice B/C/D regressions under the bundled Node runtime. Log as work proceeds using `action — exact SHA — exact command/check — observed result`.

Deliver an **implemented candidate**, not an accepted or deployed stage. Report the candidate SHA, exact observed checks, limitations, and next owner gate. Running any preflight or read model against an isolated representative production backup remains a separate authorization gate.

---
