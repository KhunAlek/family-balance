# Fresh Chat Prompt — Step 7 Shared Management Protocol

Work in the Family Cash Flow repository and implement **Step 7: Shared Management Protocol** from the uploaded package.

The expected starting repository SHA is `ca37a16c7c24f090f68baa12c5ee7333e2aecba3`.

First read repository `AGENTS.md` and every uploaded package document. Verify the package manifest, exact starting SHA, and dirty-worktree state. Inspect the exact-SHA revision/write protocol, request receipts, identity/version/component/audit constraints, canonical transaction and both history readers, Worker routing/auth, disabled mutation gates, portable backup/restore, and relevant tests before describing current behavior.

I accept the Step 6 candidate and `STAGE_6_BALANCE_HISTORY_DECISION_RECORD.md` as implementation authority for local Step 7 work. I authorize local implementation and testing of the shared management protocol using repository/synthetic fixtures, one local candidate commit, the complete Step 7 result and execution ledger, and the Step 8 handoff package. [Owner: explicitly authorize any narrow additive request-receipt migration if desired; if omitted, stop before schema changes that existing accepted structures cannot support.]

I do not authorize production or representative-copy access; production D1/R2 mutation; backfill; guessed classification/identity persistence; factual repair; observation invalidation; enabling correction/delete/restore/undo/replacement for any transaction kind; balance-history mutation; changing current dashboard, Available, commitments, reports, salary-cycle, weekly, accounting, or reconciliation consumers; dependency or Cloudflare configuration changes; merge; deployment; or Step 8/later work.

Implement authoritative zero-write previews, stale revision/terminal validation, semantic request identity and exact replay, concurrency exclusion, atomic immutable version/component/audit/receipt assembly, and rollback infrastructure. Keep every transaction kind mutation-disabled and prove direct/old-client commit attempts write nothing. Preserve all Steps 1–6 invariants.

Proceed continuously through implementation, verification, candidate commit, result, ledger, and Step 8 ZIP. Stop only for a genuine unresolved material rule, conflicting owner change, unavailable evidence, or required authorization gate; ask exactly one focused question if needed.

At the exact candidate SHA, run focused protocol preview/stale/replay/race/rollback/disabled-kind/restore tests, Steps 1–6 focused suites, and complete Slice B/C/D regressions under the bundled Node runtime. Log `action — exact SHA — exact command/check — observed result`.

Deliver an **implemented candidate**, not an accepted or deployed stage. Report the candidate SHA, exact checks, limitations, and next owner gate. Representative-backup reads, migration execution, transaction-family enablement, merge, deployment, and production access remain separately gated.
